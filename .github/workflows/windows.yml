name: Windows

on: [workflow_dispatch, pull_request, push]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{matrix.config.name}}
    runs-on: windows-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        config:
           - { name: "static library and runtime", shared: OFF, static_runtime: ON, triplet: "x64-windows-static"}
           - { name: "shared library", shared: ON, static_runtime: OFF, triplet: "x64-windows"}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Run CMake
        working-directory: ${{github.workspace}}
        # Dependencies from vcpkg.json are installed by the vcpkg toolchain
        # during configuration; the build-type is needed for VCPKG
        run: |
             mkdir build
             cd build
             cmake .. `
                   -DBUILD_SHARED_LIBS=${{ matrix.config.shared }} `
                   -DUSE_STATIC_RUNTIME=${{ matrix.config.static_runtime }} `
                   -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake `
                   -DVCPKG_TARGET_TRIPLET=${{matrix.config.triplet}} `
                   -DBUILD_TESTING=ON `
                   -DLIBSG_IMGUI_DIRECTX=ON -DLIBSG_IMGUI_OPENGL=ON `
                   -DCMAKE_BUILD_TYPE=Release -DIPO=ON -DARCH_NATIVE=ON -DLIBSG_STACKTRACE=ON

      - name: Build
        working-directory: ${{github.workspace}}/build
        run: cmake --build . --config Release

      - name: Install
        working-directory: ${{github.workspace}}/build
        run: cmake --install . --prefix ../prefix

      - name: Test
        working-directory: ${{github.workspace}}/build
        run: .\test\Release\sg_test.exe -d yes
        # ctest --output-on-failure
